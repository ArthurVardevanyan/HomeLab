- hosts: ryzen
  become: true
  user: arthur
  tasks:
    - name: Install Docker
      apt:
        state: latest
        name:
          - docker.io
          - docker-compose
    - name: Add user to docker.
      user:
        name: arthur
        groups: docker
        append: yes
