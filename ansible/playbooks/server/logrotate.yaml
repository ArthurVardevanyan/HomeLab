- hosts: AVNAS
  gather_facts: no
  become: true
  user: arthur
  tasks:
    - name: install mysql-client
      apt:
        state: latest
        name:
          - mariadb-client
    - name: Create MySql Logrotate Config
      copy:
        src: "../../../machineConfigs/server/etc/logrotate.d/mysqlbackup"
        dest: "/etc/logrotate.d/mysqlbackup"
