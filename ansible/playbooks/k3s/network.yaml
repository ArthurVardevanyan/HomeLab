- hosts: k3s
  gather_facts: no
  become: true
  user: arthur
  tasks:
    - name: Modify Network Config
      copy:
        src: "../../../machineConfigs/k3s/etc/NetworkManager/NetworkManager.conf"
        dest: "/etc/NetworkManager/NetworkManager.conf"
