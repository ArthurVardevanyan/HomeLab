- hosts: worker
  gather_facts: no
  become: true
  user: arthur
  tasks:
    - name: Modify Network Config
      copy:
        src: "../../../machineConfigs/server/etc/NetworkManager/NetworkManager.conf"
        dest: "/etc/NetworkManager/NetworkManager.conf"
