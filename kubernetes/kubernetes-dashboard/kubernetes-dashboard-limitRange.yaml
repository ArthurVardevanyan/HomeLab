apiVersion: v1
kind: LimitRange
metadata:
  name: kubernetes-dashboard-lr
  namespace: kubernetes-dashboard
spec:
  limits:
    - type: Container
      default:
        cpu: 250m
        memory: 300M
      defaultRequest:
        cpu: 25m
        memory: 50M
