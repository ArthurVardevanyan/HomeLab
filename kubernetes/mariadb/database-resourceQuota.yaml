apiVersion: v1
kind: ResourceQuota
metadata:
  name: database-rq
  namespace: database
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "1G"
    #requests.cpu: "350m"
    #requests.memory: "750M"
    #requests.cpu: "125m"
    #requests.memory: "75M"
