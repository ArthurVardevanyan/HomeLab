apiVersion: apps/v1
kind: Deployment
metadata:
  name: mariadb
  namespace: database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mariadb
  revisionHistoryLimit: 0
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mariadb
      annotations:
        use-metadata.version-checker.io/mariadb: "true"
    spec:
      containers:
        - image: mariadb:10.6.4
          imagePullPolicy: IfNotPresent
          name: mariadb
          resources:
            requests:
              memory: "750M"
              cpu: "350m"
            limits:
              memory: "1G"
              cpu: "1500m"
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                configMapKeyRef:
                  key: PASSWORD
                  name: env
          ports:
            - containerPort: 3306
              name: mariadb
          volumeMounts:
            - name: mariadb-data
              mountPath: /var/lib/mysql
            - name: mariadb-config
              mountPath: /etc/mysql/my.cnf
              subPath: my.cnf
      volumes:
        - name: mariadb-config
          configMap:
            name: mariadb-config
        - name: mariadb-data
          persistentVolumeClaim:
            claimName: mariadb-data
