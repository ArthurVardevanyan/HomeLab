apiVersion: v1
kind: LimitRange
metadata:
  name: database-lr
  namespace: database
spec:
  limits:
    - type: Container
      default:
        cpu: 250m
        memory: 300M
      defaultRequest:
        cpu: 25m
        memory: 50Mi
