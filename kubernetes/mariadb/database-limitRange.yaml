apiVersion: v1
kind: LimitRange
metadata:
  name: database-lr
  namespace: database
spec:
  limits:
    - type: Container
      default:
        cpu: 50m
        memory: 100M
      defaultRequest:
        cpu: 25m
        memory: 50Mi
