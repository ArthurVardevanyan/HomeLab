apiVersion: v1
kind: LimitRange
metadata:
  name: postgres-lr
  namespace: postgres
  labels:
    app.kubernetes.io/instance: postgres
spec:
  limits:
    - type: Container
      default:
        cpu: 30m
        memory: 64Mi
      defaultRequest:
        cpu: 15m
        memory: 32Mi
