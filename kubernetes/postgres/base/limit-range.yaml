apiVersion: v1
kind: LimitRange
metadata:
  name: postgres-lr
  namespace: postgres
  labels:
    app.kubernetes.io/instance: postgres
spec:
  limits:
    - type: Container
      default:
        cpu: 50m
        memory: 50Mi
      defaultRequest:
        cpu: 25m
        memory: 25Mi
