kind: Service
apiVersion: v1
metadata:
  name: kube-eagle
  namespace: kube-system
  labels:
    app: kube-eagle
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: kube-eagle
