apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: vault
spec:
  type: ClusterIP
  ports:
    - name: vault
      protocol: TCP
      port: 443
      targetPort: 8200
  selector:
    app: vault
