---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - traefik.yaml
  - network-policy.yaml
generatorOptions: {}
configMapGenerator:
  - files:
      - vault.json
    name: vault-config
    namespace: vault
