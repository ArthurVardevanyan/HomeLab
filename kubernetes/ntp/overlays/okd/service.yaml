apiVersion: v1
kind: Service
metadata:
  name: ntp-okd
  namespace: ntp
  labels:
    app: ntp-rootless
spec:
  ports:
    - name: metrics
      port: 9433
      targetPort: metrics
      protocol: TCP
  selector:
    app: ntp-rootless
  type: ClusterIP
