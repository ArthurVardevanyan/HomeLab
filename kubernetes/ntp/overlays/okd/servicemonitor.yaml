---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ntp-rootless
  namespace: ntp
spec:
  selector:
    matchLabels:
      app: ntp-rootless
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
