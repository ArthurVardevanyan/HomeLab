apiVersion: k8s.ovn.org/v1
kind: EgressFirewall
metadata:
  name: default
  namespace: mariadb-galera
spec:
  egress:
    - type: Allow
      to:
        cidrSelector: 10.0.0.3/32
    - type: Deny
      to:
        cidrSelector: 0.0.0.0/0
