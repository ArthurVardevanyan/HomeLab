apiVersion: apps/v1
kind: Deployment
metadata:
  name: homeassistant
  namespace: homeassistant
spec:
  replicas: 1
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app: homeassistant
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: homeassistant
      annotations:
        use-metadata.version-checker.io/homeassistant: "true"
    spec:
      containers:
        - image: homeassistant/home-assistant:2021.9.7
          imagePullPolicy: IfNotPresent
          name: homeassistant
          ports:
            - containerPort: 8123
          resources:
            requests:
              memory: "500M"
              cpu: "150m"
            limits:
              memory: "750M"
              cpu: "300m"
          volumeMounts:
            - mountPath: /config
              name: homeassistant
      hostname: homeassistant
      restartPolicy: Always
      hostNetwork: true
      volumes:
        - name: homeassistant
          persistentVolumeClaim:
            claimName: homeassistant
