apiVersion: v1
kind: Service
metadata:
  namespace: heimdall
  name: heimdall
spec:
  selector:
    app: heimdall
  ports:
    - name: web
      protocol: TCP
      port: 7075
      targetPort: 80
  type: LoadBalancer
  externalTrafficPolicy: Local
