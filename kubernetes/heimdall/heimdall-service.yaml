apiVersion: v1
kind: Service
metadata:
  namespace: heimdall
  name: heimdall
spec:
  selector:
    app: heimdall
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
  type: LoadBalancer
  externalTrafficPolicy: Local
