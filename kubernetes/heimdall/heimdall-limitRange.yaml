apiVersion: v1
kind: LimitRange
metadata:
  name: heimdall-lr
  namespace: heimdall
spec:
  limits:
    - type: Container
      default:
        cpu: 250m
        memory: 300M
      defaultRequest:
        cpu: 25m
        memory: 50M
