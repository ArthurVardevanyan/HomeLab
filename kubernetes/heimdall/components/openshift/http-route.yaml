apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: heimdall
  namespace: heimdall
  annotations:
    externaldns.k8s.io: pihole
spec:
  parentRefs:
    - name: https-gateway
      namespace: openshift-ingress
  hostnames: ["heimdall.arthurvardevanyan.com"]
  rules:
    - backendRefs:
        - name: heimdall
          namespace: heimdall
          port: 80
