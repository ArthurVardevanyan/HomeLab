apiVersion: v1
kind: LimitRange
metadata:
  name: gitlab-lr
  namespace: gitlab
spec:
  limits:
    - type: Container
      default:
        cpu: 250m
        memory: 300M
      defaultRequest:
        cpu: 25m
        memory: 50M
