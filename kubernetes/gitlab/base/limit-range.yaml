apiVersion: v1
kind: LimitRange
metadata:
  name: gitlab-lr
  namespace: gitlab
  labels:
    app.kubernetes.io/instance: gitlab
spec:
  limits:
    - type: Container
      default:
        cpu: 50m
        memory: 100M
      defaultRequest:
        cpu: 25m
        memory: 50M
