apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  annotations:
    ingress.operator.openshift.io/owned: ""
    argocd.argoproj.io/sync-wave: "0"
  labels:
    operators.coreos.com/servicemeshoperator3.openshift-operators: ""
  name: servicemeshoperator3
  namespace: openshift-operators
spec:
  channel: stable
  config:
    annotations:
      openshift.io/required-scc: restricted-v2
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    resources:
      requests:
        cpu: 10m
        memory: 64Mi
      # requests: # OverRidden By Cluster
      #   cpu: 10m
      #   memory: 256Mi
      # limits:
      #   cpu: 50m
      #   memory: 512Mi
  installPlanApproval: Manual
  name: servicemeshoperator3
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: servicemeshoperator3.v3.1.0 # servicemeshoperator3.v3.2.0 # OverRidden By Cluster
