apiVersion: v1
kind: LimitRange
metadata:
  name: tekton-lr
  namespace: tekton-pipelines
  labels:
    app.kubernetes.io/instance: tekton
spec:
  limits:
    - type: Container
      default:
        cpu: 50m
        memory: 200Mi
      defaultRequest:
        cpu: 25m
        memory: 100Mi
