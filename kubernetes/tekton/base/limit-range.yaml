apiVersion: v1
kind: LimitRange
metadata:
  name: tekton-lr
  namespace: openshift-pipelines
  labels:
    app.kubernetes.io/instance: tekton
spec:
  limits:
    - type: Container
      default:
        cpu: 30m
        memory: 128Mi
      defaultRequest:
        cpu: 10m
        memory: 64Mi
