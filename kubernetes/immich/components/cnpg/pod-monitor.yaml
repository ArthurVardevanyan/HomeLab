---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cnpg-immich
  namespace: immich
  labels:
    app.kubernetes.io/instance: immich
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: immich
  podMetricsEndpoints:
    - port: metrics
