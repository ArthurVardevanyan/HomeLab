apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  namespace: gitlab-system
  name: gitlab-system
  labels:
    app.kubernetes.io/instance: gitlab-system
spec:
  targetNamespaces:
    - gitlab-system
