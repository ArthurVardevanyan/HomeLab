kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: gitlab-config-pvc
  namespace: gitlab-system
  labels:
    app.kubernetes.io/instance: gitlab-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
  volumeName: gitlab-config-pv
  storageClassName: longhorn-static
  volumeMode: Filesystem
---
kind: PersistentVolume
apiVersion: v1
metadata:
  name: gitlab-config-pv
  labels:
    app.kubernetes.io/instance: gitlab-system
spec:
  capacity:
    storage: 10Mi
  csi:
    driver: driver.longhorn.io
    volumeHandle: gitlab-config
    fsType: ext4
    volumeAttributes:
      numberOfReplicas: "2"
  accessModes:
    - ReadWriteOnce
  claimRef:
    kind: PersistentVolumeClaim
    name: gitlab-config-pvc
    namespace: gitlab-system
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn-static
  volumeMode: Filesystem
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: gitlab-data-pvc
  namespace: gitlab-system
  labels:
    app.kubernetes.io/instance: gitlab-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
  volumeName: gitlab-data-pv
  storageClassName: longhorn-static
  volumeMode: Filesystem
---
kind: PersistentVolume
apiVersion: v1
metadata:
  name: gitlab-data-pv
  labels:
    app.kubernetes.io/instance: gitlab-system
spec:
  capacity:
    storage: 15Gi
  csi:
    driver: driver.longhorn.io
    volumeHandle: gitlab-data
    fsType: ext4
    volumeAttributes:
      numberOfReplicas: "2"
  accessModes:
    - ReadWriteOnce
  claimRef:
    kind: PersistentVolumeClaim
    name: gitlab-data-pvc
    namespace: gitlab-system
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn-static
  volumeMode: Filesystem
