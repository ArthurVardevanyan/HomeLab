apiVersion: v1
kind: Secret
metadata:
  name: photoprism-secrets
  namespace: photoprism
stringData:
  PHOTOPRISM_ADMIN_PASSWORD: <PHOTOPRISM_ADMIN_PASSWORD>
  PHOTOPRISM_DATABASE_DSN: photoprism:<PHOTOPRISM_DB_PASSWORD>@tcp(mariadb.mariadb:3306)/photoprism?charset=utf8mb4,utf8&parseTime=true
