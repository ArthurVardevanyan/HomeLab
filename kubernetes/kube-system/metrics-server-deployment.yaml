spec:
  template:
    spec:
      nodeSelector:
        node-type: master
      containers:
        - name: metrics-server
          args:
            - "--metric-resolution=15s"
            - "--cert-dir"
            - /tmp
