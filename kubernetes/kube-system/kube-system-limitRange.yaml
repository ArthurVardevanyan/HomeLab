apiVersion: v1
kind: LimitRange
metadata:
  name: kube-system-lr
  namespace: kube-system
spec:
  limits:
    - type: Container
      default:
        cpu: 100m
        memory: 170M
      defaultRequest:
        cpu: 35m
        memory: 50M
