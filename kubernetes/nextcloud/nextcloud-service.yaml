apiVersion: v1
kind: Service
metadata:
  namespace: nextcloud
  name: nextcloud
spec:
  selector:
    app: nextcloud
  ports:
    - protocol: TCP
      port: 7070
      targetPort: 7070
  type: LoadBalancer
  externalTrafficPolicy: Local
