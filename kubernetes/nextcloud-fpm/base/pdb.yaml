apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: nextcloud
  namespace: nextcloud-fpm
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: nextcloud
      db: nextcloud-pg
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: nginx
  namespace: nextcloud-fpm
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: nginx
