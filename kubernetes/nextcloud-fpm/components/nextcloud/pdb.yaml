apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: nextcloud
  namespace: nextcloud-fpm
spec:
  minAvailable: 50%
  selector:
    matchLabels:
      app: nextcloud
      db: nextcloud-pg
