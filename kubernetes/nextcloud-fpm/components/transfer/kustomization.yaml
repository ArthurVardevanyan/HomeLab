apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ./cronjob.yaml
  - ./rbac.yaml
  - ./network-policy.yaml
generatorOptions: {}
configMapGenerator:
  - files:
      - nextcloud.sh
    name: nextcloud-transfer
    namespace: nextcloud-fpm
