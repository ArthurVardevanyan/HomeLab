---
apiVersion: v1
kind: LimitRange
metadata:
  name: default
  namespace: velero
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 5m
        memory: 128Mi
      default:
        cpu: 100m
        memory: 1Gi
