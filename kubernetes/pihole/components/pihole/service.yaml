---
apiVersion: v1
kind: Service
metadata:
  name: pihole-okd
  namespace: pihole
  labels:
    app: pihole-okd
    app.kubernetes.io/instance: pihole
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: web
    - name: metrics
      port: 9617
      protocol: TCP
      targetPort: metrics
  selector:
    app: pihole
