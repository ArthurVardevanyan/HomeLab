apiVersion: v1
kind: LimitRange
metadata:
  name: prometheus-lr
  namespace: prometheus
spec:
  limits:
    - type: Container
      default:
        cpu: 250m
        memory: 300M
      defaultRequest:
        cpu: 25m
        memory: 50M
