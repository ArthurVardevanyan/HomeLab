kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: bitwarden-pvc
  namespace: bitwarden
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Mi
  volumeName: bitwarden-pv
  storageClassName: longhorn-static
  volumeMode: Filesystem
---
kind: PersistentVolume
apiVersion: v1
metadata:
  name: bitwarden-pv
spec:
  capacity:
    storage: 50Mi
  csi:
    driver: driver.longhorn.io
    volumeHandle: bitwarden
    fsType: ext4
    volumeAttributes:
      diskSelector: k3s-server-ssd
      migratable: "false"
      nodeSelector: ""
      numberOfReplicas: "2"
      staleReplicaTimeout: "20"
  accessModes:
    - ReadWriteMany
  claimRef:
    kind: PersistentVolumeClaim
    name: bitwarden-pvc
    namespace: bitwarden
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn-static
  volumeMode: Filesystem
