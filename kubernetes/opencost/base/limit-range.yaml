apiVersion: v1
kind: LimitRange
metadata:
  name: opencost-lr
  namespace: opencost
  labels:
    app.kubernetes.io/instance: opencost
spec:
  limits:
    - type: Container
      default:
        cpu: 50m
        memory: 100M
      defaultRequest:
        cpu: 25m
        memory: 50M
