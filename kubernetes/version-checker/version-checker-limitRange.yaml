apiVersion: v1
kind: LimitRange
metadata:
  name: version-checker-lr
  namespace: version-checker
spec:
  limits:
    - type: Container
      default:
        cpu: 250m
        memory: 300M
      defaultRequest:
        cpu: 25m
        memory: 50M
