---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: knowledge-sync
  namespace: llm
  labels:
    app.kubernetes.io/name: knowledge-sync
    app.kubernetes.io/instance: knowledge-sync
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: knowledge-sync
  endpoints:
    - port: http
      path: /metrics
