---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oikb-data
  namespace: llm
  labels:
    app.kubernetes.io/name: oikb
    app.kubernetes.io/instance: oikb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: rook-ceph-block
