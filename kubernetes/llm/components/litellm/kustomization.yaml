---
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - deployment.yaml
  - service.yaml
  - service-account.yaml
  - vpa.yaml

configMapGenerator:
  - name: litellm-config
    namespace: llm
    files:
      - litellm.yaml
