---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: llm-embed-models
  namespace: llm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: rook-ceph-block
  volumeMode: Filesystem
