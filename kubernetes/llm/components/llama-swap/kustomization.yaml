---
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - deployment.yaml
  - service.yaml
  - service-account.yaml
  - pvc.yaml
  - network-policy.yaml
  - vpa.yaml

configMapGenerator:
  - name: llama-swap-config
    namespace: llm
    files:
      - llama-swap.yaml
