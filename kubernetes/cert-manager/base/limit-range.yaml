apiVersion: v1
kind: LimitRange
metadata:
  name: cert-manager-lr
  namespace: cert-manager
  labels:
    app.kubernetes.io/instance: certificate-manager
spec:
  limits:
    - type: Container
      default:
        cpu: 50m
        memory: 300M
      defaultRequest:
        cpu: 25m
        memory: 150M
