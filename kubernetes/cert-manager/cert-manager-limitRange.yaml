apiVersion: v1
kind: LimitRange
metadata:
  name: cert-manager-lr
  namespace: cert-manager
spec:
  limits:
    - type: Container
      default:
        cpu: 25m
        memory: 100M
      defaultRequest:
        cpu: 10m
        memory: 75M
