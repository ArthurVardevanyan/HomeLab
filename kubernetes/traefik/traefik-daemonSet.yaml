apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app: traefik
  name: traefik
  namespace: traefik
spec:
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app: traefik
  template:
    metadata:
      annotations:
        enable.version-checker.io/traefik: "true"
        seccomp.security.alpha.kubernetes.io/defaultProfileName: runtime/default
      labels:
        app: traefik
    spec:
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsGroup: 65532
        fsGroup: 65532
        runAsUser: 65532
      containers:
        - args:
            - --global.checknewversion
            - --entryPoints.traefik.address=:9000/tcp
            - --entryPoints.web.address=:8000/tcp
            - --entryPoints.websecure.address=:8443/tcp
            - --entryPoints.registry.address=:5050/tcp
            - --entryPoints.mariadb.address=:3306/tcp
            - --api.dashboard=true
            - --ping=true
            - --providers.kubernetescrd
            - --providers.kubernetesingress
            - --api.dashboard=true
            - --log.level=INFO
            - --providers.kubernetesingress.ingressclass=traefik-internal
            - --serversTransport.insecureSkipVerify=true
            - --api.insecure=true
          image: traefik:v2.6.0
          imagePullPolicy: IfNotPresent
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /ping
              port: 9000
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          name: traefik
          ports:
            - containerPort: 9100
              name: metrics
              protocol: TCP
            - containerPort: 9000
              name: traefik
              protocol: TCP
            - containerPort: 8000
              name: web
              protocol: TCP
            - containerPort: 8443
              name: websecure
              protocol: TCP
            - containerPort: 80 # Serves No Purpose other than to Satisfy kics IaC Scanner.
              name: unused-80
            - containerPort: 443 # Serves No Purpose other than to Satisfy kics IaC Scanner.
              name: unused-443
            - containerPort: 5050 # Serves No Purpose other than to Satisfy kics IaC Scanner.
              name: unused-5050
            - containerPort: 50500 # Serves No Purpose other than to Satisfy  kicsIaC Scanner.
              name: unused-50500
          readinessProbe:
            failureThreshold: 1
            httpGet:
              path: /ping
              port: 9000
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          resources:
            limits:
              cpu: 200m
              memory: 150M
            requests:
              cpu: 50m
              memory: 75M
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsGroup: 65532
            runAsUser: 65532
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop:
                - ALL
              add:
                - NET_BIND_SERVICE
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          volumeMounts:
            - mountPath: /data
              name: data
            - mountPath: /tmp
              name: tmp
      dnsPolicy: ClusterFirst
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          effect: NoSchedule
        - key: node-role.kubernetes.io/master
          effect: NoSchedule
      restartPolicy: Always
      schedulerName: default-scheduler
      serviceAccountName: traefik
      terminationGracePeriodSeconds: 60
      volumes:
        - emptyDir: {}
          name: data
        - emptyDir: {}
          name: tmp
