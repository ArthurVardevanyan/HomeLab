apiVersion: v1
kind: LimitRange
metadata:
  name: traefik-lr
  namespace: traefik
spec:
  limits:
    - type: Container
      default:
        cpu: 10m
        memory: 10M
      defaultRequest:
        cpu: 5m
        memory: 5M
