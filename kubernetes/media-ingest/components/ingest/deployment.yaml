---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-ingest
  namespace: media-ingest
  annotations:
    argocd.argoproj.io/sync-wave: "4"
    # gpu-toolbox:not_latest is a floating tag rebuilt on every containerfile
    # change (see .tekton/gpu-toolbox.yaml) — a SHA pin would go stale on the
    # next build. Matches the same exemption used by gpu-power-manager.yaml.
    gitops-ci.k8s.io/exempt-image-checksum: "registry.arthurvardevanyan.com/homelab/gpu-toolbox:not_latest"
spec:
  # replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: media-ingest
  template:
    metadata:
      labels:
        app: media-ingest
    spec:
      automountServiceAccountToken: false
      dnsPolicy: ClusterFirst
      enableServiceLinks: false
      restartPolicy: Always
      schedulerName: default-scheduler
      serviceAccountName: ingest
      securityContext:
        runAsNonRoot: true
        supplementalGroups:
          - 44 # video
          - 107 # render
          - 797 # irc (device node group)
      volumes:
        - name: work
          emptyDir:
            sizeLimit: 200Gi
        - name: tmp
          emptyDir:
            sizeLimit: 5Gi
        - name: secret
          secret:
            secretName: rclone
        - name: config
          configMap:
            name: ingest
      containers:
        - name: ingest
          imagePullPolicy: Always
          image: registry.arthurvardevanyan.com/homelab/gpu-toolbox:not_latest
          # renovate: datasource=docker depName=registry.arthurvardevanyan.com/homelab versioning=loose
          command:
            - /bin/bash
            - /etc/config/ingest.sh
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
              gpu.intel.com/xe: "4"
            limits:
              cpu: "2"
              memory: "6Gi"
              ephemeral-storage: 200Gi
              gpu.intel.com/xe: "4"
          envFrom:
            - configMapRef:
                name: ingest
          volumeMounts:
            - name: work
              mountPath: /mnt/work
            - name: tmp
              mountPath: /tmp
            - name: secret
              mountPath: /etc/secret
              readOnly: true
            - name: config
              mountPath: /etc/config
              readOnly: true
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            privileged: false
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop:
                - ALL
      nodeSelector:
        feature.node.kubernetes.io/custom-intel.xe.gpu: "true"
      tolerations:
        - key: node-role.kubernetes.io/gpu
          operator: Exists
          effect: NoSchedule
