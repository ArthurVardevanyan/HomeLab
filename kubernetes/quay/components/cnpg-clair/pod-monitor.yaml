---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cnpg-clair
  namespace: quay
  labels:
    app.kubernetes.io/instance: quay
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: clair
  podMetricsEndpoints:
    - port: metrics
