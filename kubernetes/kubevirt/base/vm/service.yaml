apiVersion: v1
kind: Service
metadata:
  name: fedora
  namespace: arthurvardevanyan
  annotations:
    kube-vip.io/loadbalancerIPs: "10.0.0.133"
spec:
  type: LoadBalancer
  ports:
    - port: 22
      targetPort: 22
      protocol: TCP
  selector:
    vm.kubevirt.io/name: fedora
