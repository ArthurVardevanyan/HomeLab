apiVersion: v1
kind: Service
metadata:
  name: bmc-shim
  namespace: bmc-shim
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
  selector:
    app: bmc-shim
