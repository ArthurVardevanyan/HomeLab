---
kind: PodDisruptionBudget
apiVersion: policy/v1
metadata:
  name: unifi-webhook
  namespace: external-dns
  labels:
    app: unifi-webhook
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: unifi-webhook
