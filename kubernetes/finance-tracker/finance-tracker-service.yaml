apiVersion: v1
kind: Service
metadata:
  name: finance-tracker
  namespace: finance-tracker
spec:
  selector:
    app: finance-tracker
  ports:
    - protocol: TCP
      port: 7073
      targetPort: 80
  type: LoadBalancer
  externalTrafficPolicy: Local
