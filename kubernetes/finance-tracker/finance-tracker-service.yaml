apiVersion: v1
kind: Service
namespace: finance-tracker
metadata:
  name: finance-tracker
spec:
  selector:
    app: finance-tracker
  ports:
    - protocol: TCP
      port: 7073
      targetPort: 80
  type: LoadBalancer
  externalTrafficPolicy: Local
