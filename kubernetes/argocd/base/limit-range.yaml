apiVersion: v1
kind: LimitRange
metadata:
  name: argocd-lr
  namespace: argocd
  labels:
    app.kubernetes.io/instance: argocd
spec:
  limits:
    - type: Container
      default:
        cpu: 50m
        memory: 300M
      defaultRequest:
        cpu: 25m
        memory: 150M
