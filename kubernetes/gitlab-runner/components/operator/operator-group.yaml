apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: gitlab-runner
  namespace: gitlab-runner
  labels:
    app.kubernetes.io/instance: gitlab-runner
spec:
  targetNamespaces:
    - gitlab-runner
