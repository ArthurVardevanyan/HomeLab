apiVersion: v1
kind: ResourceQuota
metadata:
  name: longhorn-rq
  namespace: longhorn-system
spec:
  hard:
    requests.cpu: "3"
    requests.memory: "2G"
    services.nodeports: "0"
    services.loadbalancers: "0"
    requests.storage: "0"
    ephemeral-storage: "0"
