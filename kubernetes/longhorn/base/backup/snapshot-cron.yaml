apiVersion: longhorn.io/v1beta1
kind: RecurringJob
metadata:
  name: snapshot
  namespace: longhorn-system
  labels:
    app.kubernetes.io/instance: longhorn-system
spec:
  concurrency: 1
  cron: 0 5 * * *
  groups:
    - backup
  retain: 3
  task: snapshot
