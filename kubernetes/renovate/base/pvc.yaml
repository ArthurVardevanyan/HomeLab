---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: cache
  namespace: renovate
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi
  storageClassName: rook-ceph-block-ci
  volumeMode: Filesystem
